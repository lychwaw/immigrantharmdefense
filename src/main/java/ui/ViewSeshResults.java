package ui;
import javafx.scene.control.Label;
import javafx.scene.layout.VBox;

public class ViewSeshResults {
    public VBox getContent() {
        VBox vbox = new VBox(10);
        vbox.getChildren().add(new Label("place holder so i can continue"));
        return vbox;
    }
}